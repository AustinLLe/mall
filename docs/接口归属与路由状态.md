# 接口归属与路由状态清单

> 成员 5（接口接入与路由）交付物
> 更新时间：2026-08-31
> 依据：`docs/microservices-split.md` + 各服务 `*Controller.java` 实际代码

## 一、服务与端口

| 服务 | 端口 | 数据库 | 内部接口前缀 |
|---|---|---|---|
| `user-service` | 8081 | `user_db` | `/internal/users/**` |
| `catalog-service` | 8082 | `catalog_db` | `/internal/products/**`、`/internal/stores/**` |
| `trade-service` | 8083 | `trade_db` | `/internal/orders/**`、`/internal/sellers/**` |
| `interaction-service` | 8084 | `interaction_db` | — |

内部接口 `/internal/**` 只允许服务间直连，**不经过 Nginx 公网入口**（当前 Nginx 无 `/internal/` 路由，天然隔离）。

## 二、路由状态（Nginx 已落地）

### 已切换到微服务（`deploy/nginx/default.conf` 已配置）

| 前端路径 | 方法 | 目标服务 | 微服务实现状态 |
|---|---|---|---|
| `/api/users` | POST/GET | `user-service` | 已实现（创建/查询用户） |
| `/api/users/{id}` | GET | `user-service` | 已实现 |
| `/api/products` | GET/POST | `catalog-service` | 已实现（发布/查询商品） |
| `/api/products/{id}` | GET | `catalog-service` | 已实现 |
| `/api/orders` | POST | `trade-service` | 已实现（跨服务下单） |
| `/api/orders/{id}` | GET | `trade-service` | 已实现 |
| `/api/topics` | GET/POST | `interaction-service` | 已实现（话题创建/列表） |
| `/api/topics/{id}` | GET | `interaction-service` | 已实现 |

### 仍兜底到旧单体（微服务尚未实现，暂不切换）

| 前端路径 | 归属服务（目标） | 原因 |
|---|---|---|
| `/api/auth/**`（register/login/me/profile/search-users） | user-service | 注册登录/资料/鉴权未迁移 |
| `/api/address/**` | user-service | 地址管理未迁移 |
| `/api/center/**` | user-service（聚合）+ catalog（items） | 用户中心/实名认证未迁移 |
| `/api/cart/**` | trade-service | 购物车未迁移 |
| `/api/stores/**` | catalog-service | 店铺/收藏/足迹/关注未迁移 |
| `/api/admin/audit/**` | catalog-service | 商品审核未迁移 |
| `/api/upload/image` | catalog-service | 图片上传未迁移 |
| `/api/chat/**`、`/api/ai/**`、`/ws/**` | interaction-service | 聊天/AI/WebSocket 未迁移 |

## 三、路由匹配规则

采用 **Nginx 前缀匹配 + 最长前缀优先**：

1. 具体前缀（`/api/users`、`/api/products`、`/api/orders`、`/api/topics`）先命中，转发到对应微服务。
2. 兜底 `location /api/` 命中其余所有接口，转发到旧单体 `backend:8080`。
3. `proxy_pass` 不带 URI 后缀，原始路径完整透传，前后端契约不变。

## 四、兼容性规范（成员 5 已提供，待成员 1-4 落地）

### 1. 响应格式：已提供 `ApiResult` 规范模板

| 端 | 返回格式 |
|---|---|
| 旧单体 | `ApiResult{ code, message, data }` 统一包装，`code=0` 成功 |
| 微服务（现状） | 裸数据（`List` / 对象），无包装 |

前端 `utils/request.js` 先取 `res.data`（包装对象）再取 `.data`；微服务裸返回会导致取值路径不一致。

**已落地**：`services/common/ApiResult.java`（与旧单体字段完全一致）。成员 1-4 复制到 `com.example.mall.common` 包，所有返回值改 `ApiResult.ok(...)`；错误继续抛 `ResponseStatusException`，由全局异常处理统一转 `ApiResult`。

### 2. 鉴权：已提供 `AuthClient` 过渡方案

| 端 | 鉴权方式 |
|---|---|
| 旧单体 | `Authorization: Bearer <token>`，后端查内存映射得到用户 |
| 微服务（现状） | `POST /api/orders` 直接传明文 `buyerId/addressId`，不认 token |

**关键约束**：旧单体 token 是「随机串 → 用户名」的**进程内存映射**，token 不含用户信息，微服务无法本地解析。

**已落地（过渡方案）**：`services/common/AuthClient.java` —— 微服务把 token 转发回旧单体 `/api/auth/me` 校验并取用户。Nginx 已透传 `Authorization` 头。

**最终方案（待团队拍板）**：升级为 JWT 自包含 token，各服务用共享密钥本地解析、不再回源。需改旧单体登录逻辑 + 各服务加 JWT 校验，由成员 1 牵头、全员配合。

### 3. 目录重复

仓库同时存在 `services/`（根 `pom.xml` 聚合）与 `shopping_back/{user,catalog,trade,interaction}-service/` 两套微服务目录。**以 `services/` 为准**，需清理 `shopping_back/` 下的重复目录，避免团队各自改错位置。

## 五、部署前提

1. 本 Nginx 配置的 upstream 使用服务名 `user-service` 等，**依赖** 4 个微服务的 K8s Service（或 docker-compose 服务）已注册。
2. 当前 `k8s/kustomization.yaml` 尚未包含 4 个微服务的资源清单，需运维/成员补交 `user-service.yaml` 等 Deployment + Service 后方可生效。
3. 本地 `docker-compose.yml` 也需补充 4 个微服务容器 + 各自的数据库（`user_db`/`catalog_db`/`trade_db`/`interaction_db`）初始化，否则本地 Nginx 启动时 upstream 解析失败。
