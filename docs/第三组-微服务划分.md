# 微服务划分
## 微服务划分图
<img src="D:\Study\大二下\软工\大作业\小学期\.assets\image-20260828233025937.png" alt="image-20260828233025937" style="zoom:50%;" />

> 本文档描述微服务改造后的目标架构、接口归属和数据归属。当前代码仍为单体版本；后续改造时，各服务应能独立构建、测试和部署。

服务职责说明

|服务|主要职责|划分理由|
|:-:|:-:|:-:|
|用户服务 `user-service`|注册登录、身份认证、个人资料、实名认证、信用、收货地址、用户管理|都围绕用户身份及用户生命周期，安全要求和数据一致性相近|
|商品服务 `catalog-service`|商品、店铺、收藏、浏览记录、关注店铺、商品审核、图片上传|商品和店铺是交易平台的核心目录，查询量大，适合作为独立扩容单元|
|交易服务 `trade-service`|购物车、下单、订单状态、取消订单、评价|具有明确交易事务边界，后续可单独进行订单压力测试|
|互动与智能服务 `interaction-service`|话题、帖子、评论、点赞、关注话题、帖子行为、会话、消息、已读状态、AI 议价、AI 助手、WebSocket|社区互动、即时会话与智能能力均属于用户互动域，与交易核心流程耦合较低|

## 服务接口清单

> 本清单描述改造后的目标接口归属。为降低前端改造成本，原有对外 API 路径原则上保持不变；服务内部调用统一使用 `/internal/**` 接口，内部接口不通过 API 网关对公网开放。

### 1. 用户服务

|方法|路径|功能|调用方|
|:-:|:-:|:-:|:-:|
|POST|`/api/auth/register`|注册|前端|
|POST|`/api/auth/login`|登录并返回 Token|前端|
|GET|`/api/auth/me`|查询当前用户|前端、内部服务|
|PUT|`/api/auth/me/profile`|修改个人资料|前端|
|GET|`/api/auth/search-users`|搜索用户|前端|
|GET|`/api/center/buyer`|买家中心聚合信息|前端|
|GET|`/api/center/seller`|卖家中心聚合信息|前端|
|GET|`/api/center/admin`|管理员中心|前端|
|POST|`/api/center/buyer/realname`|提交买家实名认证|前端|
|PUT|`/api/center/buyer/realname/cancel`|取消买家实名认证|前端|
|POST|`/api/center/seller/realname`|提交卖家实名认证|前端|
|PUT|`/api/center/seller/realname/cancel`|取消卖家实名认证|前端|
|GET|`/api/center/buyer/items/credit`|查询信用记录|前端|
|PUT|`/api/center/admin/users/{userId}/status`|修改用户状态|管理员|
|PUT|`/api/center/admin/users/{userId}/credit`|调整用户信用|管理员|
|DELETE|`/api/center/admin/users/{userId}`|删除或注销用户|管理员|
|PUT|`/api/center/admin/realname/{id}/approve`|实名审核通过|管理员|
|PUT|`/api/center/admin/realname/{id}/reject`|实名审核拒绝|管理员|
|GET|`/api/address`|地址列表|前端|
|POST|`/api/address`|新增地址|前端|
|PUT|`/api/address/{addressId}`|修改地址|前端|
|DELETE|`/api/address/{addressId}`|删除地址|前端|
|PUT|`/api/address/{addressId}/default`|设置默认地址|前端|

规划的内部服务接口：

|方法|路径|用途|
|:-:|:-:|:-:|
|GET|`/internal/users/{userId}`|查询用户公开资料和状态|
|POST|`/internal/users/batch`|批量查询用户公开信息|
|GET|`/internal/users/{userId}/addresses/{addressId}`|交易服务校验地址|
|POST|`/internal/users/{userId}/credit-records`|交易完成或处罚后增加信用记录|
### 2. 商品服务

|方法|路径|功能|
|:-:|:-:|:-:|
|GET|`/api/products`|商品列表和搜索|
|GET|`/api/products/mine`|查询自己发布的商品|
|GET|`/api/products/{id}`|商品详情|
|POST|`/api/products`|发布商品|
|PUT|`/api/products/{id}`|修改商品|
|GET|`/api/stores`|店铺列表|
|GET|`/api/stores/mine`|当前卖家的店铺|
|PUT|`/api/stores/mine`|修改当前店铺|
|GET|`/api/stores/{id}`|店铺详情|
|GET|`/api/stores/{id}/products`|店铺商品列表|
|POST|`/api/stores/{id}/follow`|关注店铺|
|DELETE|`/api/stores/{id}/follow`|取消关注店铺|
|GET|`/api/center/buyer/items/favorite`|收藏列表|
|POST|`/api/center/buyer/items/favorite`|添加收藏|
|PUT|`/api/center/buyer/items/favorite/clear`|清空收藏|
|GET|`/api/center/buyer/items/history`|浏览记录|
|POST|`/api/center/buyer/items/history`|添加浏览记录|
|PUT|`/api/center/buyer/items/history/clear`|清空浏览记录|
|GET|`/api/center/buyer/items/follow`|关注店铺列表|
|GET|`/api/admin/audit`|待审核商品列表|
|POST|`/api/admin/audit/{id}`|审核商品|
|POST|`/api/upload/image`|上传商品或店铺图片|

规划的内部服务接口：

|方法|路径|用途|
|:-:|:-:|:-:|
|GET|`/internal/products/{goodsId}/snapshot`|返回标题、价格、卖家、状态等交易快照|
|POST|`/internal/products/batch`|购物车批量获取商品摘要|
|PUT|`/internal/products/{goodsId}/sold`|成交后将商品标记为已售|
|GET|`/internal/stores/{storeId}/summary`|互动与智能服务或交易服务查询店铺摘要|
### 3. 交易服务
|方法|路径|功能|
|:-:|:-:|:-:|
|GET|`/api/cart`|查询购物车|
|POST|`/api/cart`|加入购物车|
|PUT|`/api/cart/{cartId}`|修改购买数量|
|DELETE|`/api/cart/{cartId}`|删除购物车项|
|PUT|`/api/cart/{cartId}/select`|选择或取消选择|
|GET|`/api/orders`|查询订单|
|POST|`/api/orders`|创建订单|
|POST|`/api/orders/{id}/cancel`|取消订单|
|POST|`/api/orders/{id}/review`|评价订单和商品|

规划的内部服务接口：

|方法|路径|用途|
|:-:|:-:|:-:|
|GET|`/internal/orders/{orderId}`|其他服务查询订单基本信息|
|GET|`/internal/orders/{orderId}/participants`|判断用户是否为订单参与者|
|GET|`/internal/sellers/{sellerId}/summary`|卖家中心统计订单及销售额|
### 4. 互动与智能服务
|方法|路径|功能|
|:-:|:-:|:-:|
|GET|`/api/topics`|话题列表|
|GET|`/api/topics/{id}`|话题详情|
|POST|`/api/topics`|创建话题|
|POST|`/api/topics/{id}/follow`|关注话题|
|DELETE|`/api/topics/{id}/follow`|取消关注话题|
|GET|`/api/topics/{id}/posts`|话题帖子列表|
|POST|`/api/topics/{id}/posts`|发布帖子|
|POST|`/api/topic-posts/{id}/comments`|发布评论|
|POST|`/api/topic-posts/{id}/like`|点赞或取消点赞|
|POST|`/api/topic-posts/{id}/action`|收藏、分享等帖子行为|
|GET|`/api/chat/conversations`|会话列表|
|POST|`/api/chat/conversations`|创建会话|
|GET|`/api/chat/conversations/{covId}/messages`|查询消息|
|POST|`/api/chat/conversations/{covId}/messages`|发送消息|
|POST|`/api/chat/conversations/{covId}/transfer`|转人工|
|POST|`/api/chat/conversations/{covId}/ai-bargain`|AI 议价|
|POST|`/api/chat/{covId}/read`|标记已读|
|PUT|`/api/chat/conversations/{covId}/status`|修改会话状态|
|DELETE|`/api/chat/conversations/{covId}`|删除会话|
|POST|`/api/ai/assistant`|通用 AI 助手|
|POST|`/api/ai/assist`|商品问答与议价建议|
|POST|`/api/ai/publish-suggestion`|商品发布建议|
|WebSocket|`/ws/**`|实时消息推送|

互动与智能服务内部仍按社区、会话和 AI 能力保持模块化代码组织；本次合并仅调整服务部署边界，不改变现有对外 API 路径。

## 数据表归属方案

当前数据库一共 20 张业务表。

|数据库/Schema|所属服务|数据表|说明|
|:-:|:-:|:-:|:-:|
|`user_db`|用户服务|`users`|用户账号、角色、状态和信用总分|
|`user_db`|用户服务|`user_realname_auth`|实名认证申请和审核|
|`user_db`|用户服务|`credit_record`|信用变更明细|
|`user_db`|用户服务|`user_address`|收货地址|
|`catalog_db`|商品服务|`goods`|商品及审核状态|
|`catalog_db`|商品服务|`store`|店铺信息|
|`catalog_db`|商品服务|`favorite_goods`|用户收藏商品关系|
|`catalog_db`|商品服务|`browse_history`|商品浏览记录|
|`catalog_db`|商品服务|`follow_store`|用户关注店铺关系|
|`trade_db`|交易服务|`cart_item`|购物车|
|`trade_db`|交易服务|`orders`|订单|
|`trade_db`|交易服务|`product_review`|订单和商品评价|
|`interaction_db`|互动与智能服务|`community_topic`|社区话题|
|`interaction_db`|互动与智能服务|`topic_post`|帖子|
|`interaction_db`|互动与智能服务|`topic_comment`|评论|
|`interaction_db`|互动与智能服务|`topic_post_like`|点赞关系|
|`interaction_db`|互动与智能服务|`topic_post_action`|收藏、分享等行为|
|`interaction_db`|互动与智能服务|`follow_topic`|关注话题关系|
|`interaction_db`|互动与智能服务|`conversation`|买卖双方会话|
|`interaction_db`|互动与智能服务|`chat_message`|聊天和 AI 消息|

### 数据访问约束

1. 每张业务表只能由所属服务直接读写，其他服务不能直接访问该表。
2. 各服务可以共用一个 MySQL 实例，但应使用不同的数据库或 Schema。
3. 服务之间只保存逻辑业务 ID，不建立跨数据库外键，也不执行跨库 JOIN。
4. 跨服务数据访问必须通过 REST 接口、事件或消息完成。
5. 订单应保存下单时的商品名称、成交价格等必要快照，避免商品后续修改影响历史订单。

## 跨服务调用与失败处理

### 1. 创建订单

调用流程：

1. 前端通过 API 网关调用交易服务创建订单。
2. 交易服务调用用户服务，校验用户状态和收货地址归属。
3. 交易服务调用商品服务，查询商品快照，并校验商品是否审核通过、是否可售。
4. 交易服务保存订单以及商品名称、成交价格等交易快照。
5. 交易服务调用商品服务，将对应商品标记为已售。

失败处理：

- 用户服务不可用时，交易服务在超时后终止下单，不写入订单。
- 商品服务不可用、商品不存在或商品不可售时，不创建订单。
- 订单已经创建，但商品标记为已售失败时，记录补偿任务并进行有限重试；最终失败则取消订单。
- 创建订单请求携带唯一业务号，保证重复请求或重试不会产生重复订单。

### 2. AI 议价

调用流程：

1. 前端通过 API 网关调用互动与智能服务发起 AI 议价。
2. 互动与智能服务调用商品服务，查询商品状态、当前价格和最低价。
3. 互动与智能服务生成议价建议并保存消息。
4. 互动与智能服务通过 WebSocket 将结果推送给用户。

失败处理：

- 调用商品服务时设置超时时间，避免请求长期阻塞。
- 商品服务暂时不可用时，优先使用短期缓存的商品摘要。
- 没有可用缓存时，返回“商品信息暂时不可用，请稍后重试”的备用结果。
- AI 模型不可用时，使用规则生成备用议价建议，普通聊天功能仍然保持可用。

该场景可用于课程要求的故障处理实验：主动停止商品服务，验证 AI 议价返回预先设计的备用结果，并确认其他服务不会发生级联故障。

### 3. 买家中心和卖家中心

调用流程：

1. 用户服务查询用户基本信息、实名认证和信用数据。
2. 用户服务调用商品服务，获取收藏、足迹、关注店铺和发布商品统计。
3. 用户服务调用交易服务，获取订单数量、待处理订单和销售额。
4. 用户服务聚合各服务结果后返回前端。

失败处理：

- 某个下游服务不可用时，仍然返回用户基本信息。
- 不可用模块的数据暂时置空，并返回 `partial=true` 和 `unavailableModules`。
- 聚合接口不因单个依赖服务发生故障而整体失败。
- 商品、订单等列表数据通过批量接口查询，避免逐条远程调用。

### 4. 社区内容展示

调用流程：

1. 互动与智能服务调用用户服务，批量查询帖子作者的公开信息。
2. 互动与智能服务调用商品服务，批量查询帖子关联的商品和店铺摘要。

失败处理：

- 用户服务不可用时，使用短期缓存或显示匿名作者。
- 商品服务不可用时，隐藏关联商品卡片，但帖子正文仍然正常展示。
- 使用批量查询接口和短期缓存，避免列表页面产生大量跨服务调用。

