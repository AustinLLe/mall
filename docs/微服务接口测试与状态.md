# 微服务接口测试与迁移状态

## 1. 文档目的

本文用于记录 `master` 分支根目录 `services/` 下四个业务微服务的接口迁移、HTTP API 测试、跨服务联调和阻塞状态。

> 测试范围仅包含 `services/user-service`、`services/catalog-service`、`services/trade-service` 和 `services/interaction-service`。`shopping_back/*-service` 下的另一套合入代码不作为本组当前测试对象。

原有 `tests/api/soft-shop-api.postman_collection.json` 继续作为旧单体完整 API 回归基线；微服务测试使用 `tests/api/soft-shop-microservices.postman_collection.json`，两者不能互相覆盖。

## 2. 测试环境

| 项目 | 内容 |
|---|---|
| 测试分支 | `master` |
| 初始基线提交 | `b4409b7` |
| 当前同步提交 | `812ce17` |
| Java | 21.0.8（项目目标版本 Java 17） |
| Spring Boot | 3.5.13 |
| 单元测试 | Maven、JUnit 5、H2 |
| API 测试 | Postman、Newman |
| 运行数据库 | MySQL 8.x，四个逻辑数据库 |
| 测试日期 | 2026-08-31 |

## 3. 微服务清单

| 服务 | 默认端口 | 独占数据库 | 当前已实现切片 | 负责人 |
|---|---:|---|---|---|
| `user-service` | 8081 | `user_db` | 注册登录、资料、地址、用户中心、内部用户查询 | 成员1 |
| `catalog-service` | 8082 | `catalog_db` | 商品发布/修改/查询、关系、店铺、内部商品接口 | 成员2 |
| `trade-service` | 8083 | `trade_db` | 购物车、订单列表/创建/取消/评价及内部订单接口 | 成员3 |
| `interaction-service` | 8084 | `interaction_db` | 话题、帖子、评论、点赞和帖子行为 | 成员4 |
| 统一入口与路由 | 8080（暂定） | 无 | Nginx/API 路由 | 成员5 |
| 接口测试与状态 | — | — | Collection、Newman 报告、状态跟踪 | 成员6 |

## 4. 状态定义

| 状态 | 判定规则 |
|---|---|
| 待迁移 | 新服务中尚无对应接口 |
| 开发中 | 负责人正在迁移，接口契约尚未冻结 |
| 待联调 | 已有实现，但运行环境、依赖或路由尚未完成 |
| 测试中 | 已编写并开始运行 HTTP API 测试 |
| 已通过 | 服务真实运行，HTTP 状态和关键响应断言全部通过并保存报告 |
| 阻塞 | 存在明确的外部前置条件，当前无法继续 |

“代码存在”或“单元测试通过”不能直接标记为“接口已通过”。

## 5. 单元测试基线

执行命令：

```powershell
.\services\test-services.ps1
```

2026-08-31 基线结果：

| 服务 | 测试数 | 失败 | 结果 |
|---|---:|---:|---|
| `user-service` | 2 | 0 | 通过 |
| `catalog-service` | 1 | 0 | 通过 |
| `trade-service` | 4 | 0 | 通过 |
| `interaction-service` | 1 | 0 | 通过 |
| 合计 | 8 | 0 | `BUILD SUCCESS` |

日志中的 Mockito/Byte Buddy Java Agent 信息为 Java 21 兼容性警告，不影响本次结果。

## 6. 接口迁移与测试状态

| 编号 | 服务 | 方法 | 路径 | 初始状态 | 最后测试时间 | 说明/阻塞原因 |
|---|---|---|---|---|---|---|
| MS-USER-001 | user | POST | `/api/users` | 待联调 | — | 当前为基础用户创建，并非最终注册接口 |
| MS-USER-002 | user | GET | `/api/users/{id}` | 待联调 | — | 依赖 MS-USER-001 |
| MS-USER-003 | user | GET | `/internal/users/{id}` | 待联调 | — | 服务间内部接口 |
| MS-USER-004 | user | GET | `/internal/users/{userId}/addresses/{addressId}` | 待联调 | — | 地址公开接口已迁移，可自动创建前置数据 |
| MS-USER-005 | user | POST | `/api/auth/register` | 待联调 | — | 已迁入 user-service，仍为内存 Token |
| MS-USER-006 | user | POST | `/api/auth/login` | 待联调 | — | 已迁入 user-service，JWT 尚未完成 |
| MS-USER-007 | user | ALL | `/api/address/**` | 待联调 | — | 地址增删改查及默认地址已迁移 |
| MS-CATALOG-001 | catalog | POST | `/api/products` | 待联调 | — | 当前接受请求体中的 `sellerId` |
| MS-CATALOG-002 | catalog | GET | `/api/products` | 待联调 | — | 商品列表 |
| MS-CATALOG-003 | catalog | GET | `/api/products/{id}` | 待联调 | — | 依赖 MS-CATALOG-001 |
| MS-CATALOG-004 | catalog | GET | `/internal/products/{id}/snapshot` | 待联调 | — | 交易服务依赖接口 |
| MS-CATALOG-005 | catalog | PUT | `/internal/products/{id}/sold` | 待联调 | — | 会改变商品状态，默认不在回归中执行 |
| MS-TRADE-001 | trade | POST | `/api/orders` | 待联调 | — | 新契约为 Bearer Token + `items[]`，无需请求体地址 |
| MS-TRADE-002 | trade | GET | `/api/orders` | 待联调 | — | 订单列表已实现 |
| MS-TRADE-003 | trade | ALL | `/api/cart/**` | 待联调 | — | 购物车列表、加购、数量、勾选、删除已实现 |
| MS-INTERACTION-001 | interaction | POST | `/api/topics` | 待联调 | — | 改为 Bearer Token，输入为 title/desc/type/cover/tags |
| MS-INTERACTION-002 | interaction | GET | `/api/topics` | 待联调 | — | 话题列表 |
| MS-INTERACTION-003 | interaction | GET | `/api/topics/{id}` | 待联调 | — | 依赖 MS-INTERACTION-001 |
| MS-INTERACTION-004 | interaction | ALL | `/api/topics/{id}/posts` | 待联调 | — | 帖子列表和创建已实现 |
| MS-INTERACTION-005 | interaction | POST | `/api/topic-posts/{id}/comments|like|action` | 待联调 | — | 评论、点赞和行为已实现 |
| MS-ROUTE-001 | gateway | ALL | `/api/**` | 阻塞 | — | 业务路由已配置，但用户域接口仍兜底旧单体 |

## 7. API 测试执行方式

### 7.1 Postman

导入：

```text
tests/api/soft-shop-microservices.postman_collection.json
```

服务直连变量默认值：

```text
user_url=http://127.0.0.1:8081
catalog_url=http://127.0.0.1:8082
trade_url=http://127.0.0.1:8083
interaction_url=http://127.0.0.1:8084
```

Collection 会先在 user-service 注册买家和卖家、创建地址，再依次完成商品、购物车、订单和互动流程。当前直连联调要求将 catalog/trade/interaction 的 `AUTH_BASE_URL` 指向 user-service；否则新 user-service 签发的 Token 无法被另外三个服务识别。

### 7.2 Newman

四个服务启动后执行：

```powershell
npx newman run .\tests\api\soft-shop-microservices.postman_collection.json `
  --reporters cli,htmlextra,json,junit `
  --reporter-htmlextra-export .\tests\api\reports\microservices\newman-report.html `
  --reporter-json-export .\tests\api\reports\microservices\newman-report.json `
  --reporter-junit-export .\tests\api\reports\microservices\newman-report.xml
```

如测试统一入口，将四个 `*_url` 变量都覆盖为成员5提供的统一地址。

## 8. 关键测试流程

### 8.1 基础服务流程

```text
创建买家 → 查询买家 → 内部查询买家
创建卖家 → 创建商品 → 商品列表 → 商品详情 → 商品快照
创建话题 → 话题列表 → 话题详情
```

### 8.2 跨服务订单流程

```text
准备买家和买家地址
→ 创建在售商品
→ trade-service 调用 user-service 校验用户和地址
→ trade-service 调用 catalog-service 获取商品快照
→ trade-service 在 trade_db 创建订单快照
→ catalog-service 幂等标记商品已售
→ 查询订单并校验 CONFIRMED
```

重点断言：

1. 相同 `clientRequestId` 重复提交不会创建两条订单。
2. 商品不存在时返回明确错误。
3. 用户或地址不存在时创建失败。
4. 商品服务不可用时返回 503，不能生成错误的已确认订单。
5. 订单保存商品名称和成交价格快照。

## 9. 问题与阻塞记录

| 编号 | 时间 | 问题 | 影响 | 责任人 | 状态 |
|---|---|---|---|---|---|
| BLOCK-001 | 2026-08-31 | Nginx 的 `/api/auth`、`/api/address`、`/api/center` 仍兜底旧单体 | 无法从统一入口证明用户接口已迁移 | 成员5 | 阻塞 |
| BLOCK-002 | 2026-08-31 | Docker Compose 中三个下游服务的 `AUTH_BASE_URL` 仍指向旧 `backend:8080` | user-service 新 Token 无法用于商品、交易、互动服务 | 成员1/5 | 阻塞 |
| BLOCK-003 | 2026-08-31 | 认证仍为进程内随机 Token，尚未升级 JWT | user-service 多副本和重启会使 Token 失效 | 成员1 | 开发中 |
| BLOCK-004 | 2026-08-31 | Kubernetes 尚无四个微服务 Deployment/Service | 暂不能验证集群统一入口和 HPA | 部署负责人 | 开发中 |
| BLOCK-005 | 2026-08-31 | user-service 测试启动时重复执行 `ALTER TABLE users ADD role` | H2 已有 `role` 列，2项用户服务测试均在加载上下文时出错 | 成员1 | 阻塞 |

## 10. 每日结果记录

| 日期时间 | Git 提交 | 测试范围 | 请求/断言 | 通过 | 失败 | 跳过 | 报告路径 | 结论 |
|---|---|---|---:|---:|---:|---:|---|---|
| 2026-08-31 11:12 | `b4409b7` | 四服务单元测试 | 8项测试 | 8 | 0 | 0 | Maven控制台/Surefire | 基线通过，HTTP API待执行 |
| 2026-08-31 | `812ce17` | 同步最新 master 并更新接口契约 | 21个HTTP请求 | — | — | — | `soft-shop-microservices.postman_collection.json` | 等待按最新环境执行 |
| 2026-08-31 16:27 | `812ce17` | 最新微服务单元测试 | 23项测试 | 21 | 2 | 0 | Maven控制台/Surefire | catalog 6、trade 13、interaction 2通过；user 2项发生启动错误 |

## 11. 接口交付信息模板

业务成员将接口交给测试时，应提供：

```text
所属服务：
请求方法和路径：
请求体示例：
成功响应示例：
失败状态码：
是否需要登录/角色：
依赖的前置数据或服务：
涉及数据库表：
代码提交号：
本地验证命令：
```

只有在真实 HTTP 请求、状态码断言、关键字段断言均通过并保存 Newman 报告后，才将状态改为“已通过”。
