# 松果集市测试方案与用例

> 文档版本：v2.0
>
> 适用系统：松果集市新品与二手商品交易平台
>
> 编写日期：2026-08-25
>
> 文档性质：测试执行依据（测试结果必须以报告、截图或日志为准）

## 1. 测试目标

本轮测试验证系统的功能正确性、接口稳定性、页面业务流程、并发性能、安全性和部署可靠性。测试覆盖前端、后端、MySQL 数据库及前后端之间的调用链路。

## 2. 测试工具与分工

| 工具 | 测试类型 | 主要测试内容 | 输出证据 |
|---|---|---|---|
| Docker / Docker Compose | 部署与环境测试 | 服务构建、容器启动、健康检查、重启恢复、数据持久化 | 容器状态、启动日志、访问截图 |
| JUnit 5 + Mockito | 白箱、单元测试 | 后端服务类的正常、异常和边界分支 | Maven 测试报告 |
| JaCoCo | 白箱覆盖率 | 统计行覆盖率、分支覆盖率和未覆盖代码 | HTML 覆盖率报告 |
| Postman | 黑箱、接口测试 | 接口状态码、响应结构、参数校验、鉴权和权限隔离 | Collection Runner 报告 |
| Selenium | 前端自动化测试 | 用户在浏览器中的完整业务流程 | 执行报告、失败截图 |
| JMeter | 性能与压力测试 | 登录、商品查询、店铺查询、用户中心等接口的并发能力 | HTML 性能报告、JTL 文件 |
| OWASP ZAP | 安全测试 | 常见 Web 漏洞和安全响应头检查 | ZAP 扫描报告 |
| Chrome、Edge | 兼容性与人工探索测试 | 页面布局、交互、异常提示和浏览器兼容性 | 测试记录、截图 |

## 3. 测试环境

### 3.1 基础环境

| 项目 | 测试配置 |
|---|---|
| 操作系统 | Windows 11 或 Linux 服务器 |
| 前端地址 | `http://127.0.0.1:5173` 或测试服务器地址 |
| 后端地址 | `http://127.0.0.1:8080` 或测试服务器地址 |
| 数据库 | MySQL 8.0，独立测试库 `shop_db` |
| Java | JDK 21 |
| 浏览器 | Chrome、Edge 最新稳定版 |
| 测试账号 | 普通买家、卖家、管理员各一个 |

### 3.2 测试前置要求

1. 前端、后端和 MySQL 均已启动，后端 8080 端口可访问。
2. 测试库包含可展示商品、店铺和测试账号。
3. 测试使用独立环境，不对正式生产数据执行新增、删除、压力或安全扫描。
4. 每轮测试记录代码提交号、测试时间、执行人员和工具版本。
5. 涉及新增、下单、审核、删除的用例执行后恢复测试数据。

## 4. Docker 部署测试

Docker 用于验证项目在统一、可重复的环境中能够正确部署和恢复。

| 编号 | 实际测试用例 | 操作 | 预期结果 |
|---|---|---|---|
| DEP-01 | 镜像构建 | 构建前端和后端镜像 | 构建成功，无依赖缺失或编译错误 |
| DEP-02 | 完整环境启动 | 使用 Docker Compose 启动 MySQL、后端和前端 | 所有容器均处于运行或健康状态 |
| DEP-03 | 后端连库 | 查看后端启动日志并访问 `/api/products` | 数据库连接成功，接口返回业务数据 |
| DEP-04 | 前端访问 | 访问前端首页 | 页面正常显示，静态资源无 404 |
| DEP-05 | 前后端通信 | 在首页加载商品并执行登录 | 请求成功，浏览器无连接拒绝和跨域错误 |
| DEP-06 | 容器重启恢复 | 重启后端容器后再次访问商品接口 | 服务可恢复，接口结果正常 |
| DEP-07 | 数据持久化 | 新增一条测试数据，重启 MySQL 容器后查询 | 数据仍然存在，数据卷工作正常 |
| DEP-08 | 错误配置检查 | 在测试环境使用错误数据库密码启动后端 | 后端启动失败并输出明确的数据库认证错误 |

证据保存：容器列表截图、后端启动日志、首页截图和接口响应截图。

## 5. JUnit、Mockito 与 JaCoCo 白箱测试

### 5.1 已建立的认证服务单元用例

对应代码：`shopping_back/shopping_back/src/test/java/com/example/shopping_back/auth/AuthServiceTest.java`。

| 编号 | 被测逻辑 | 输入或模拟条件 | 预期结果 |
|---|---|---|---|
| UT-AUTH-01 | 正确账号登录 | 模拟用户存在、密码正确、状态正常 | 返回非空 Token，用户名和角色正确，手机号被脱敏 |
| UT-AUTH-02 | 错误密码登录 | 模拟用户存在但密码错误 | 抛出 401 未授权异常 |
| UT-AUTH-03 | 冻结账号登录 | 模拟账号状态为 `disabled` | 抛出 403 禁止访问异常 |
| UT-AUTH-04 | 非法注册管理员 | 注册参数中的角色为 `admin` | 抛出 400 参数错误，不能通过普通注册获得管理员身份 |

### 5.2 后续应补充的核心单元用例

| 编号 | 被测模块 | 实际测试场景 | 预期结果 |
|---|---|---|---|
| UT-PROD-01 | 商品查询 | 数据访问层返回多个商品 | 服务正确转换并返回商品列表 |
| UT-PROD-02 | 商品详情 | 查询不存在的商品 ID | 返回明确的不存在异常 |
| UT-PROD-03 | 商品发布 | 合法卖家提交完整商品信息 | 创建商品，初始状态符合审核规则 |
| UT-PROD-04 | 商品发布 | 标题为空、价格小于等于 0 | 拒绝保存并返回参数错误 |
| UT-ORD-01 | 订单创建 | 商品存在且可售 | 创建订单并计算正确金额 |
| UT-ORD-02 | 订单创建 | 商品已下架或库存不足 | 创建失败，不产生无效订单 |
| UT-ORD-03 | 订单评价 | 非订单买家提交评价 | 拒绝操作，订单状态不改变 |
| UT-CHAT-01 | 创建会话 | 买家针对有效商品联系卖家 | 创建或返回已有会话，避免重复会话 |
| UT-CHAT-02 | 发送消息 | 会话参与者发送合法文本 | 保存消息并返回正确发送者和时间 |
| UT-CHAT-03 | 越权发消息 | 非会话参与者发送消息 | 拒绝操作 |
| UT-ADMIN-01 | 商品审核 | 管理员审核待审商品为通过 | 商品变为可展示状态 |
| UT-ADMIN-02 | 越权审核 | 普通用户调用审核逻辑 | 拒绝操作，商品状态不改变 |

### 5.3 覆盖率要求

JaCoCo 报告位置：`shopping_back/shopping_back/target/site/jacoco/index.html`。

| 指标 | 本阶段验收值 | 重点要求 |
|---|---:|---|
| 核心服务类行覆盖率 | ≥ 70% | 认证、商品、订单、聊天、审核 |
| 核心服务类分支覆盖率 | ≥ 60% | 正常、异常、权限和边界分支 |
| 全项目行覆盖率 | ≥ 50% | 后续随重构逐步提高 |

测试结果保存：Maven 控制台日志、Surefire 报告和 JaCoCo HTML 报告首页截图。

## 6. Postman 黑箱接口测试

### 6.1 认证与鉴权

| 编号 | 请求 | 测试数据或条件 | 预期结果 |
|---|---|---|---|
| API-AUTH-01 | `POST /api/auth/register` | 合法用户名、密码、手机号和普通角色 | 注册成功，返回用户数据和 Token |
| API-AUTH-02 | `POST /api/auth/register` | 重复用户名 | 注册失败，提示用户名已存在 |
| API-AUTH-03 | `POST /api/auth/register` | 空用户名、短密码或错误手机号 | 返回参数错误，不写入用户 |
| API-AUTH-04 | `POST /api/auth/login` | 正确账号和密码 | HTTP 200，业务码为 0，Token 非空 |
| API-AUTH-05 | `POST /api/auth/login` | 错误密码 | 返回认证失败，不返回 Token |
| API-AUTH-06 | `GET /api/auth/me` | 携带有效 Bearer Token | 返回当前登录用户信息 |
| API-AUTH-07 | `GET /api/auth/me` | 不携带 Token | 返回 401 |
| API-AUTH-08 | `GET /api/auth/me` | 携带伪造或过期 Token | 返回 401，不泄露用户信息 |

### 6.2 商品、店铺与话题

| 编号 | 请求 | 测试数据或条件 | 预期结果 |
|---|---|---|---|
| API-PROD-01 | `GET /api/products` | 无筛选参数 | 返回业务码 0，商品数组非空 |
| API-PROD-02 | `GET /api/products` | 分别使用新品、二手场景筛选 | 只返回符合场景的商品 |
| API-PROD-03 | `GET /api/products` | 使用存在的关键词搜索 | 返回标题或描述匹配的商品 |
| API-PROD-04 | `GET /api/products` | 使用不存在的关键词 | 返回空数组，不返回服务器错误 |
| API-PROD-05 | `GET /api/products/{id}` | 有效商品 ID | 返回对应商品的完整信息 |
| API-PROD-06 | `GET /api/products/{id}` | 不存在或非法 ID | 返回明确的 4xx 错误 |
| API-PROD-07 | `POST /api/products` | 卖家 Token 与合法商品数据 | 创建成功，返回新商品 ID |
| API-PROD-08 | `POST /api/products` | 无 Token | 返回 401，不创建商品 |
| API-STORE-01 | `GET /api/stores` | 无参数 | 返回店铺列表 |
| API-STORE-02 | `GET /api/stores/{id}/products` | 有效店铺 ID | 仅返回该店铺的商品 |
| API-TOPIC-01 | `GET /api/topics` | 无参数 | 返回话题列表 |
| API-TOPIC-02 | `POST /api/topics/{id}/posts` | 已登录用户提交合法内容 | 帖子创建成功并归属正确话题 |

### 6.3 订单、消息与管理

| 编号 | 请求 | 测试数据或条件 | 预期结果 |
|---|---|---|---|
| API-ORD-01 | `POST /api/orders` | 买家 Token、有效商品与地址 | 订单创建成功，金额和商品正确 |
| API-ORD-02 | `GET /api/orders` | 买家 Token | 仅返回当前用户相关订单 |
| API-ORD-03 | `POST /api/orders/{id}/review` | 订单已完成且属于当前买家 | 评价成功，订单状态正确 |
| API-ORD-04 | `POST /api/orders/{id}/review` | 评价他人订单 | 返回 403，不产生评价 |
| API-CHAT-01 | `POST /api/chat/conversations` | 有效商品 ID 和买家 Token | 创建或返回对应会话 |
| API-CHAT-02 | `POST /api/chat/conversations/{id}/messages` | 会话参与者发送文本 | 消息保存成功并返回消息对象 |
| API-CHAT-03 | `GET /api/chat/conversations/{id}/messages` | 会话参与者 Token | 返回该会话消息，顺序正确 |
| API-CHAT-04 | `POST /api/chat/conversations/{id}/ai-bargain` | 有效会话 | 返回 AI 议价建议或明确的降级结果 |
| API-CHAT-05 | `POST /api/chat/conversations/{id}/transfer` | 有效会话 | 会话切换为人工模式 |
| API-CHAT-06 | 任一会话接口 | 非会话参与者 Token | 返回 403，不能读取或发送消息 |
| API-ADMIN-01 | `GET /api/admin/audit` | 管理员 Token | 返回待审核商品列表 |
| API-ADMIN-02 | `POST /api/admin/audit/{id}` | 管理员提交通过或拒绝状态 | 审核结果被保存 |
| API-ADMIN-03 | 管理审核接口 | 普通用户 Token | 返回 403，数据不改变 |

### 6.4 Postman 自动断言

每个请求至少检查：HTTP 状态码、响应时间、`Content-Type`、业务码、必要字段和字段类型。登录请求保存 Token 到测试环境变量；需要鉴权的请求统一使用该变量。集合执行时不得只看请求是否发送成功，断言失败即判定用例失败。

## 7. Selenium 浏览器端到端测试

| 编号 | 用户流程 | 自动化步骤 | 预期结果 |
|---|---|---|---|
| E2E-01 | 登录成功 | 打开登录页，输入正确账号密码并提交 | 进入首页，显示当前用户信息 |
| E2E-02 | 登录失败 | 输入错误密码并提交 | 停留在登录页并显示明确提示 |
| E2E-03 | 首页商品加载 | 登录后进入首页并等待商品区域 | 至少显示一张商品卡片，无“暂无商品”误提示 |
| E2E-04 | 商品筛选 | 依次点击全部、新品、二手 | 列表内容与筛选条件一致 |
| E2E-05 | 商品搜索与详情 | 搜索已知关键词并点击结果 | 搜索结果正确，详情页信息完整 |
| E2E-06 | 发布商品 | 卖家登录，填写表单、上传图片并提交 | 显示提交成功，新商品进入相应状态 |
| E2E-07 | 创建订单 | 买家选择商品，填写或选择地址并提交 | 订单创建成功，可在订单列表查看 |
| E2E-08 | 联系卖家 | 从商品详情进入消息页并发送文字 | 会话打开，发送的消息出现在消息区 |
| E2E-09 | 管理员审核 | 管理员进入审核页并通过一件待审商品 | 商品从待审列表移除并可在前台展示 |
| E2E-10 | 登录态失效 | 删除或替换本地 Token 后打开受限页面 | 自动跳转登录或提示重新登录 |
| E2E-11 | 刷新恢复 | 在商品详情、订单或消息页刷新浏览器 | 页面能够恢复，关键数据不丢失 |
| E2E-12 | 空数据与接口错误 | 使用空测试数据或让测试接口返回错误 | 页面显示空状态或错误提示，不出现白屏 |

执行要求：优先使用稳定的 `data-testid` 定位元素；失败时自动保存截图、当前 URL 和浏览器控制台错误。

## 8. JMeter 性能与压力测试

现有计划文件：`tests/performance/shop-api-load.jmx`，执行脚本：`tests/performance/run-jmeter.ps1`。

### 8.1 接口场景

| 编号 | 性能场景 | 请求 | 校验内容 |
|---|---|---|---|
| PERF-01 | 用户登录 | `POST /api/auth/login` | HTTP 200、Token 提取成功、无业务错误 |
| PERF-02 | 首页商品查询 | `GET /api/products` | HTTP 200、业务码为 0、列表结构正确 |
| PERF-03 | 店铺列表查询 | `GET /api/stores` | HTTP 200、响应结构正确 |
| PERF-04 | 登录后用户中心 | `GET /api/center/buyer` | 携带 Token 后返回 200，不出现越权错误 |

### 8.2 负载模型

| 阶段 | 并发用户 | 持续时间 | 目的 |
|---|---:|---:|---|
| 冒烟 | 1 | 1 分钟 | 验证脚本、参数和断言正确 |
| 基准 | 20 | 5 分钟 | 获取正常负载下的基线数据 |
| 负载 | 50、100 | 每档 10 分钟 | 验证目标并发下的稳定性 |
| 压力 | 从 100 逐步增加 | 每档 5 分钟 | 找到响应恶化或错误明显上升的拐点 |
| 稳定性 | 50 | 30 分钟以上 | 检查内存增长、连接池耗尽和长期错误 |

### 8.3 性能验收标准

| 指标 | 验收标准 |
|---|---:|
| 总错误率 | < 1% |
| 商品、店铺查询 P95 | < 500 ms |
| 登录和用户中心 P95 | < 800 ms |
| 单接口 P99 | < 1500 ms |
| 测试期间后端 CPU 平均使用率 | < 75% |
| 稳定性测试 | 无持续内存增长、服务崩溃、连接池耗尽 |

报告必须记录吞吐量、平均响应时间、P90、P95、P99、最大响应时间、错误率，以及后端 CPU、内存和数据库连接情况。压力测试失败时应保留首次超过阈值的并发数。

## 9. OWASP ZAP 安全测试

| 编号 | 安全场景 | 测试内容 | 预期结果 |
|---|---|---|---|
| SEC-01 | 未授权访问 | 扫描需要登录的用户中心、订单、消息和管理接口 | 未登录请求被拒绝 |
| SEC-02 | 越权访问 | 用普通用户访问管理员接口或他人数据 | 返回 403，敏感数据不泄露 |
| SEC-03 | 输入注入 | 在登录、搜索、发帖和消息参数中使用 SQL/XSS 测试字符串 | 不执行脚本、不改变查询逻辑、不出现数据库错误栈 |
| SEC-04 | 安全响应头 | 检查点击劫持、内容类型、缓存和 CSP 等响应头 | 高风险缺失项修复或形成审核记录 |
| SEC-05 | 敏感信息泄露 | 检查错误响应、日志和前端资源 | 不出现数据库密码、Token、堆栈和内部路径 |
| SEC-06 | 文件上传 | 上传非图片、超大文件和伪造扩展名文件 | 非法文件被拒绝，文件名与路径不可注入 |
| SEC-07 | Token 滥用 | 使用伪造、空白、过期和被修改的 Token | 全部被拒绝 |

ZAP 自动扫描用于发现风险，最终结论需要人工复核，避免把误报直接记为系统缺陷。

## 10. 浏览器兼容性与人工黑箱测试

| 编号 | 测试项目 | Chrome | Edge | 预期结果 |
|---|---|---|---|---|
| COMP-01 | 首页布局与商品卡片 | 待执行 | 待执行 | 无错位、遮挡或横向溢出 |
| COMP-02 | 登录、退出和登录态保持 | 待执行 | 待执行 | 功能一致，刷新后状态正确 |
| COMP-03 | 商品搜索、筛选和详情 | 待执行 | 待执行 | 结果一致，跳转正确 |
| COMP-04 | 发布表单与图片上传 | 待执行 | 待执行 | 校验、上传和提交一致 |
| COMP-05 | 订单与消息页面 | 待执行 | 待执行 | 数据显示完整，交互可用 |
| COMP-06 | 1366×768 与 1920×1080 | 待执行 | 待执行 | 主要按钮可见，页面可正常滚动 |

人工探索重点检查：重复点击、快速刷新、空输入、超长文本、网络中断、后端停止、接口超时、返回空数据和返回异常数据时，页面是否有明确提示且不会白屏。

## 11. 执行顺序

1. 使用 Docker 部署独立测试环境，确认基础服务可用。
2. 执行 JUnit 单元测试并生成 JaCoCo 覆盖率报告。
3. 使用 Postman 完成接口功能、异常、边界和权限测试。
4. 使用 Selenium 执行主要用户端到端流程。
5. 使用 JMeter 按冒烟、基准、负载、压力、稳定性顺序测试。
6. 使用 ZAP 扫描并人工复核安全问题。
7. 在 Chrome、Edge 中执行兼容性和人工探索测试。
8. 汇总报告、缺陷、复测结果并给出是否通过验收的结论。

## 12. 测试结果记录模板

### 12.1 用例执行记录

| 用例编号 | 执行日期 | 实际结果 | 状态 | 证据位置 | 缺陷编号 |
|---|---|---|---|---|---|
| 例：API-AUTH-04 | YYYY-MM-DD | 登录返回 200 且取得 Token | 通过/失败/阻塞 | 报告或截图路径 | 无或 BUG-001 |

状态定义：

- **通过**：实际结果与全部预期结果一致，并有可复核证据。
- **失败**：实际结果与任一预期不一致，已记录缺陷。
- **阻塞**：因环境、账号或依赖不可用而无法执行，不计为通过。
- **未执行**：尚未开始执行。

### 12.2 缺陷记录

| 缺陷编号 | 关联用例 | 问题描述 | 严重程度 | 复现步骤 | 状态 |
|---|---|---|---|---|---|
| BUG-001 | 用例编号 | 实际现象与预期差异 | 致命/严重/一般/提示 | 可重复执行的步骤 | 新建/修复中/待复测/已关闭 |

### 12.3 最终测试报告至少包含

1. 测试环境、代码提交号和测试时间。
2. 各类用例的总数、通过数、失败数、阻塞数和通过率。
3. JUnit、Postman、Selenium、JMeter、ZAP 的原始报告或截图。
4. JaCoCo 覆盖率和 JMeter P95、P99、吞吐量、错误率。
5. 未解决缺陷及其影响。
6. 明确结论：通过验收、有条件通过或不通过。

未经执行的用例不得填写为通过；修复后的缺陷必须执行原用例和相关回归用例后才能关闭。
