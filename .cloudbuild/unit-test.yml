---
version: 2.0

env:
  resource:
    type: docker
    arch: X86
    class: 2U8G

steps:
  PRE_BUILD:
    - checkout:
        name: checkout
        inputs:
          scm: codehub
          url: https://codehub.devcloud.cn-north-4.huaweicloud.com/a111c887c5a545eda4c6976ba10c1a75/NewSecondMall.git
          branch: ${codeBranch}
          lfs: true
          submodule: true
          depth: 1

  BUILD:
    - maven:
        name: unit-test
        image: cloudbuild@maven3.9.5-jdk17
        inputs:
          cache: false
          command: |-
            set -euo pipefail
            export DB_PASSWORD="${CI_DB_PASSWORD:?CI_DB_PASSWORD is required}"
            export MYSQL_ROOT_PASSWORD="${CI_MYSQL_ROOT_PASSWORD:?CI_MYSQL_ROOT_PASSWORD is required}"
            if [ ! -f shopping_back/shopping_back/pom.xml ]; then
              pom="$(find . /data /var /workspace /tmp -path '*/shopping_back/shopping_back/pom.xml' 2>/dev/null | head -n 1)"
              if [ -z "${pom:-}" ]; then
                echo "找不到仓库根目录"
                exit 1
              fi
              cd "$(dirname "$(dirname "$(dirname "$pom")")")"
            fi
            cd shopping_back/shopping_back
            mvn -B -ntp test jacoco:report
            cd ../..
            tar -czf unit-test-surefire.tgz -C shopping_back/shopping_back/target surefire-reports
            if [ -d shopping_back/shopping_back/target/site/jacoco ]; then
              tar -czf unit-test-jacoco.tgz -C shopping_back/shopping_back/target/site jacoco
            fi
    - upload_artifact:
        name: upload-unit-surefire
        inputs:
          path: "unit-test-surefire.tgz"
          custom_upload_path: /NewSecondMall-unit-test/
          ignore_fail: true
    - upload_artifact:
        name: upload-unit-jacoco
        inputs:
          path: "unit-test-jacoco.tgz"
          custom_upload_path: /NewSecondMall-unit-test/
          ignore_fail: true
